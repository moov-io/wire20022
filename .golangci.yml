version: "2"
run:
  modules-download-mode: readonly
linters:
  enable:
    - bodyclose
    - dupword
    - forbidigo
    - forcetypeassert
    - misspell
    - unconvert
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
